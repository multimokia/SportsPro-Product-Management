@model assignment1.ViewModels.IncidentViewModel

@{
    ViewData["Title"] = "Delete";
}

<div class="jumbotron jumbotron-fluid">
  <div class="container">
    <h1 class="title-custom display-4">Delete</h1>
    <p class="lead" style="text-align:center;">Are you sure you want to delete this?</p>
</div>
</div>

<div class="container">

    <dl class="row wrapper">
        <dt class = "col-sm-6 d-flex justify-content-left">
            @Html.DisplayNameFor(model => model.Title)
        </dt>
        <dd class = "col-sm-6 d-flex justify-content-left">
            @Html.DisplayFor(model => model.Title)
        </dd>
        <dt class = "col-sm-6 d-flex justify-content-left">
            @Html.DisplayNameFor(model => model.Description)
        </dt>
        <dd class = "col-sm-6 d-flex justify-content-left">
            @Html.DisplayFor(model => model.Description)
        </dd>
        <dt class = "col-sm-6 d-flex justify-content-left">
            @Html.DisplayNameFor(model => model.DateOpened)
        </dt>
        <dd class = "col-sm-6 d-flex justify-content-left">
            @Html.DisplayFor(model => model.DateOpened)
        </dd>
        <dt class = "col-sm-6 d-flex justify-content-left">
            @Html.DisplayNameFor(model => model.DateClosed)
        </dt>
        <dd class = "col-sm-6 d-flex justify-content-left">
            @Html.DisplayFor(model => model.DateClosed)
        </dd>
    </dl>

            <form asp-action="Delete">
               <input type="hidden" asp-for="IncidentId" />
                    <div class="form-group button_center">
                  <input type="submit" class=" btn btn-danger" value="Delete" style="margin:0.5rem;" />
                  <button type="button" class=" btn btn-form" style="margin:0.5rem;">
                       <a asp-action="Index" >Back to List</a>
                  </button>
            </div>
    </form>

</div>
